// in order to use a component you need to import it first
import RecipeCard from './components/RecipeCard';

// declare our App component that returns some JSX
const App = () => {
  return <RecipeCard />;
};

export default App;
